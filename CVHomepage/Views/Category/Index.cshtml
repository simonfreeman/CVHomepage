@model IEnumerable<CVHomepage.Models.Category>

@{
    ViewBag.Title = "Category Manager";
}

<h1>Category Manager</h1>

<p>
    @*@Html.ActionLink("Create New", "Create")*@
    You can view categories here, but right now tag management is off limits while I decide the best
    way of dealing with a global category system that doesn't succumb to vandalism/user-error/chaos.
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Description)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Description)
        </td>
        @*<td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ID }) |
            @Html.ActionLink("Details", "Details", new { id=item.ID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ID })
        </td>*@
    </tr>
}

</table>
